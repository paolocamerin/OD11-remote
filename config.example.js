/**
 * Optional: copy to config.local.js to set your speaker IP without --ip flag.
 * config.local.js is gitignored.
 *
 * Or run with: node index.js --ip=192.168.0.101
 */
module.exports = {
    speakerIp: 'YOUR_SPEAKER_IP'  // e.g. 192.168.0.101 or hostname
};
